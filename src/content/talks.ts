import type { Talk } from './schema';

// Talks are not surfaced as a category yet (Projects & Publications only).
// Kept as an empty, typed collection so the schema/aggregator stay stable.
export const talks: Talk[] = [];
